
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# apps/api — local dev
PORT=3000
WEB_ORIGIN=http://localhost:5173

# Test accounts used by apps/api/src/__tests__ (create via Supabase Admin API
# or dashboard, then insert a matching public.users row — see Sprint 2)
TEST_VOLUNTEER_EMAIL=
TEST_VOLUNTEER_PASSWORD=
TEST_ADMIN_EMAIL=
TEST_ADMIN_PASSWORD=

# apps/web — local dev (Vite requires these to also live in apps/web/.env)
VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
